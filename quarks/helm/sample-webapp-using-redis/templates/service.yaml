---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name | quote }}
  labels:
    {{- include "quarks.labels" . | nindent 4 }}
    app.kubernetes.io/component: "service"
spec:
  type: ClusterIP
  selector:
    fissile.cloudfoundry.org/instance-group-name: sample-webapp-using-redis
    fissile.cloudfoundry.org/deployment-name: {{ .Values.deployment_name }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8080
